pragma solidity 0.4.24;

// We import the contract so truffle compiles it, and we have the ABI 
// available when working from truffle console; though we could also just 
// initialize the contract class from the build artifact in the gnosis safe 
// package instead of recompiling it.
import "gnosis-safe/contracts/GnosisSafePersonalEdition.sol";
